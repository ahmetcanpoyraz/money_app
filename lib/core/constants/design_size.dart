import 'package:flutter/material.dart';

class AppDesignSize {
  AppDesignSize._();

  static Size portraitDesignSize = const Size(375, 812);
  static Size landspaceDesignSize = const Size(812, 375);
}
